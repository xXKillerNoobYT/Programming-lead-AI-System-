#!/usr/bin/env node
// Architecture-lint placeholder for Phase 3 §A.1 (D-20260418-008).
// §A.5 fills this in with real architecture invariants (dependency graph,
// layer boundaries, disallowed imports). Exits 0 until then.
//
// Invocation: `npm run check:arch`
console.log('[check:arch] Architecture lint placeholder — §A.5 will implement real invariants.');
process.exit(0);
